spring:
#  messages:
#    basename: messages/message
#    encoding: UTF-8
#  thymeleaf:
#    prefix: classpath:/templates/
#    suffix: .html
  mvc:
    view:
      hiddenmethod:
        filter:
          enabled: true
      prefix: /WEB-INF/
      suffix: .jsp
    static-path-pattern: /static/**
  thymeleaf:
    enabled: false

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/demodb?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: fully&**ddblack97

  jpa:
    hibernate:
      # create로 하게 되면, table 각각을 drop해야 하는데, 테이블 2/4개만 만들어지고, 2개는 덜 만들어져서, 만들어지지 않은 2개의 테이블을 찾을 수 없다는 오류가 나온다.
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    # show-sql: true
    properties:
      hibernate.format_sql: true
      # dialect: org.hibernate.dialect.MySQL8InnoDBDialect

# 임시
tmp:
  user_id: 1
  store_id: 1
  robot_id: 1
